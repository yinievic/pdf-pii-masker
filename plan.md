# plan.md (ver.2.0)

# P2 PDF 개인(신용)정보 마스킹 웹앱 실행계획

## 작업: 수동 마스킹·자동 OCR 마스킹·자동 결과 수정/폐기 흐름을 모두 지원하는 PDF 마스킹 웹앱 개발 계획

---

## 0. 현재 진행상태

현재까지 Codex를 통해 기존 plan.md(ver.1.0)의 Step 1부터 Step 3까지 제작 및 검증을 완료한 상태로 본다.

| 완료 단계 | 상태 | 비고 |
|---|---:|---|
| Step 1. 기존 PDF 업로드 페이지 상태 확인 | 완료 | 업로드 UI 및 PDF 파일 읽기 흐름 확인 |
| Step 2. PDF.js 도입 및 페이지별 렌더링 구현 | 완료 | PDF.js canvas 렌더링, 파일별 미리보기 frame, 단일 페이지 보기/이동 UI 구현 |
| Step 3. 페이지별 상태 데이터 구조 설계 | 완료 | PageRenderState / MaskBox 기본 구조, canvas 기준 좌표 변환, 페이지별 mask 추가·삭제 함수 구현 |

ver.2.0은 기존 plan.md(ver.1.0)의 형식은 유지하되, 최종 시스템 목표를 다음과 같이 변경한다.

```text
기존 방향:
수동 마스킹 MVP 완성
→ 이후 정규식/OCR 자동 탐지 확장

변경 방향:
원본 PDF 보존
→ 수동/자동/자동결과 수정이 가능한 공통 워크플로우 설계
→ 수동 마스킹 기능 구현
→ 내부 OCR 서버 기반 자동 탐지 구현
→ OCR 결과 검토·수정·폐기 흐름 구현
→ 최종 다운로드 엔진 통합
```

---

## 1. 목표

P2의 목표는 사용자가 업로드한 PDF를 브라우저에서 페이지별로 확인하고, 수동 또는 자동 방식으로 개인(신용)정보를 마스킹한 뒤, 마스킹이 실제 반영된 이미지 기반 PDF를 다운로드할 수 있도록 하는 것이다.

최종 시스템은 다음 사용 흐름을 지원해야 한다.

1. 사용자는 PDF 업로드 후 `수동 마스킹` 또는 `자동 마스킹`을 선택할 수 있다.
2. 수동 마스킹 선택 시, 사용자가 PDF 페이지 위에서 직접 마스킹 박스를 생성·삭제·보완할 수 있다.
3. 자동 마스킹 선택 시, 내부 OCR 서버 또는 향후 확장 가능한 OCR Provider를 통해 스캔본 PDF에서 텍스트와 좌표를 탐지한다.
4. OCR 결과는 즉시 최종 확정하지 않고, 사용자가 검토할 수 있는 자동 마스킹 후보로 표시한다.
5. 사용자는 OCR 결과가 만족스러우면 그대로 다운로드할 수 있다.
6. OCR 결과가 불완전하면 다음 중 선택할 수 있다.
   - OCR 결과에 수동 마스킹을 추가·삭제·수정한 후 다운로드
   - OCR 결과를 폐기하고 원본 PDF 기준으로 수동 마스킹만 수행한 후 다운로드
7. 최종 다운로드 PDF는 원본 텍스트 레이어가 남지 않는 이미지 기반 PDF로 생성한다.
8. 원본 PDF와 결과 PDF는 원칙적으로 서버에 영구 저장하지 않는다.
9. OCR 서버 처리 도입 시에도 원본 파일, 페이지 이미지, OCR 텍스트, 결과물의 저장·로그 정책을 별도로 통제한다.

핵심 목표는 다음과 같다.

1. 원본 PDF 상태를 항상 보존한다.
2. 수동 마스킹과 자동 마스킹 결과를 동일한 `MaskBox` 구조로 관리한다.
3. OCR 결과는 `candidate`, `accepted`, `rejected` 상태로 검토 가능하게 한다.
4. 다운로드 로직은 수동/자동 여부와 무관하게 최종 선택된 `MaskBox[]`를 받아 처리하는 공통 함수로 통합한다.
5. 스캔본 PDF가 주 대상임을 전제로, 내부 OCR 서버 기반 확장 구조를 포함한다.

---

## 2. 범위

ver.2.0 계획의 포함 범위는 다음과 같다.

| 구분 | 포함 내용 |
|---|---|
| PDF 업로드 | 기존 업로드 페이지 유지 및 원본 PDF 상태 보존 |
| PDF 렌더링 | PDF.js를 사용한 페이지별 canvas 렌더링 |
| 페이지 상태 관리 | 페이지 번호, canvas 크기, scale, 렌더링 결과, 마스킹 목록 관리 |
| 공통 MaskBox 구조 | 수동/자동/OCR/정규식 탐지 결과를 동일 구조로 관리 |
| 워크플로우 상태 | 수동 모드, 자동 모드, 자동 결과 검토 모드, OCR 결과 폐기 후 수동 모드 관리 |
| 수동 마스킹 | 사용자가 드래그로 마스킹 박스 생성 |
| 마스킹 박스 관리 | 페이지별 추가·삭제·상태 변경 |
| OCR 준비 | 내부 NAS 서버 OCR API 구조 설계 |
| OCR 실행 | Tesseract kor+eng 기반 OCR 실행 구조 |
| 좌표 추출 | TSV 또는 hOCR 기반 텍스트·좌표·신뢰도 추출 |
| 정규식 탐지 | 주민등록번호, 전화번호, 이메일, 계좌번호 등 패턴 탐지 |
| 자동 후보 표시 | OCR/정규식 결과를 자동 마스킹 후보로 표시 |
| 자동 결과 수정 | OCR 결과에 수동 마스킹 추가·삭제 가능 |
| 자동 결과 폐기 | OCR 결과를 제거하고 원본 기준 수동 마스킹으로 전환 |
| PDF 재생성 | 최종 선택된 마스킹 박스를 이미지 기반 PDF에 반영 |
| 다운로드 | Blob URL 방식의 브라우저 다운로드 제공 |
| 저장 정책 | 원본·결과 파일 영구 저장 없음, OCR 임시파일은 별도 보안정책 필요 |

---

## 3. 제외범위

이번 계획에서 즉시 구현하지 않거나 별도 승인 후 진행할 항목은 다음과 같다.

| 제외 항목 | 제외 사유 |
|---|---|
| 외부 OCR API | 개인정보 외부 전송, 업무위탁, 국외이전, API 키 관리 이슈로 별도 승인 전까지 보류 |
| 외부 LLM API | 개인정보 전송 및 보안정책 검토 전까지 보류 |
| LLM 기반 비정형 판단 | 성명·주소 등 비정형 판단은 OCR/정규식 기반 구조 안정화 이후 검토 |
| 사용자 계정 기능 | 현재 개발 범위 아님 |
| 작업 이력 영구 저장 | 개인정보·감사·보존정책 확정 전까지 보류 |
| 서버 파일 영구 보관 | 보안정책 확정 전까지 원본·결과 파일 영구 저장 금지 |
| OCR 결과 DB 저장 | OCR 텍스트 자체가 개인정보일 수 있으므로 별도 승인 필요 |
| 대량 일괄 처리 | 단건 PDF 처리 안정화 이후 검토 |
| 박스 이동·리사이즈 고급 편집 | 초기에는 생성·삭제 중심, 필요 시 후속 확장 |
| 블러/부분투명 마스킹 | 비가역적 마스킹 목적상 검은 박스를 기본값으로 함 |

---

## 4. 단계별 실행계획

---

### Step 1. 기존 PDF 업로드 페이지 상태 확인

#### 작업 내용

현재 구현된 PDF 업로드 페이지의 구조를 확인한다.

확인 대상은 다음과 같다.

1. `src/App.tsx`의 현재 업로드 UI 구조
2. PDF 파일 선택 이벤트 처리 방식
3. 파일 상태값 관리 방식
4. Vite 개발 서버에서 정상 표시 여부
5. 향후 PDF.js, pdf-lib 적용 가능 구조 여부

#### 완료 기준

- PDF 파일을 선택할 수 있다.
- 선택된 PDF 파일명이 화면에 표시된다.
- 업로드된 파일을 `ArrayBuffer`로 읽을 수 있는 구조가 준비되어 있다.

#### 현재 상태

완료.

---

### Step 2. PDF.js 도입 및 페이지별 렌더링 구현

#### 작업 내용

업로드된 PDF를 PDF.js로 로드하고, 전체 페이지를 canvas로 렌더링한다.

처리 흐름은 다음과 같다.

```text
PDF 파일 선택
→ file.arrayBuffer()
→ PDF.js getDocument()
→ 전체 페이지 수 확인
→ 각 페이지 getPage()
→ viewport 생성
→ canvas 렌더링
→ 화면에 페이지별 미리보기 표시
```

#### 구현 원칙

- 서버 업로드 없이 브라우저에서 PDF를 렌더링한다.
- 각 페이지는 wrapper div 안에 배치한다.
- wrapper div는 향후 마스킹 박스를 overlay할 수 있는 구조로 만든다.
- 업로드 직후 PDF를 렌더링하되, 미리보기 영역은 `PDF 파일 페이지 확인` 버튼 클릭 후 표시한다.
- 파일별로 하나의 미리보기 frame을 생성한다.
- 각 문서는 한 번에 한 페이지씩 표시하고, 좌우 이동 버튼과 현재 페이지 숫자 입력으로 페이지를 전환한다.
- 미리보기 상단에는 파일명과 전체 페이지 수를 같은 줄에 표시한다.
- 렌더링 scale은 우선 2.0 기준으로 시작한다.

#### 완료 기준

- PDF 업로드 후 전체 페이지 수가 표시된다.
- 모든 페이지를 원본 순서대로 페이지 이동 UI에서 확인할 수 있다.
- 각 페이지가 이미지처럼 확인 가능하다.
- 페이지별 wrapper 구조가 생성되어 있다.
- 실제 표시 가능한 페이지가 있는 경우에만 `PDF 파일 페이지 확인` / `PDF 파일 마스킹` 버튼이 분할 표시된다.

#### 현재 상태

완료. 현재 구현은 `PDF 파일 페이지 확인` 버튼 클릭 후 파일별 미리보기 frame을 표시하며, 각 frame에서는 한 번에 한 페이지씩 확인한다. 좌우 페이지 이동 버튼과 현재 페이지 숫자 입력으로 이동할 수 있다.

---

### Step 3. 페이지별 상태 데이터 구조 설계

#### 작업 내용

각 페이지에 대한 렌더링 정보와 마스킹 박스 정보를 저장할 상태 구조를 설계한다.

기본 구조는 다음과 같다.

```ts
type MaskSource = "manual" | "ocr" | "regex" | "llm";

type MaskBox = {
  id: string;
  pageNumber: number;
  x: number;
  y: number;
  width: number;
  height: number;
  source?: MaskSource;
  label?: string;
};

type PageRenderState = {
  pageNumber: number;
  width: number;
  height: number;
  scale: number;
  canvasDataUrl: string;
  masks: MaskBox[];
};
```

현재 구현에서는 PDF.js 렌더링이 완료된 페이지를 대상으로 하므로 `canvasDataUrl`을 선택값이 아니라 필수값으로 둔다. `source`와 `label`은 후속 OCR/정규식/LLM 탐지 결과를 같은 구조로 수용하기 위한 선택 필드이며, `status`, `confidence`, `text`는 Step 4에서 보완한다.

#### 설계 원칙

- 마스킹 좌표는 페이지별 canvas 기준 좌표로 저장한다.
- 화면 표시 좌표와 실제 canvas 좌표의 변환 관계를 명확히 한다.
- 다운로드 시 동일 좌표를 canvas에 반영할 수 있어야 한다.
- 후속 자동 탐지 결과도 같은 `MaskBox` 구조로 저장할 수 있어야 한다.
- 화면 표시 좌표와 canvas 좌표 변환은 `displayRectToCanvasRect`, `canvasRectToDisplayRect` 같은 별도 함수로 분리한다.

#### 완료 기준

- 각 페이지별 마스킹 박스 목록을 저장할 수 있다.
- 특정 페이지의 박스만 추가·삭제할 수 있다.
- 후속 OCR/정규식 탐지 결과를 동일 구조로 확장할 수 있다.
- 특정 페이지에만 mask를 추가하는 `addMask` 함수가 준비된다.
- 특정 페이지에서 특정 mask만 삭제하는 `removeMask` 함수가 준비된다.

#### 현재 상태

완료. `PageRenderState`, `MaskBox`, canvas 기준 좌표 변환 함수, `addMask`, `removeMask`가 구현되어 있다. 실제 드래그 UI는 Step 5에서 구현한다.

---

### Step 4. 공통 MaskBox 및 워크플로우 상태 구조 보완

#### 작업 내용

수동 마스킹과 자동 OCR 마스킹을 모두 수용할 수 있도록 `MaskBox`와 전체 워크플로우 상태를 확장한다.

현재 Step 3 구현에는 `source?`, `label?` 선택 필드가 이미 반영되어 있다. Step 4에서는 자동 후보 검토와 다운로드 대상 산정을 위해 `status`, `confidence`, `text` 등 자동 마스킹용 필드와 전체 워크플로우 상태를 보완한다.

기존 `MaskBox`는 다음과 같이 확장한다.

```ts
type MaskSource = "manual" | "ocr" | "regex" | "llm";

type MaskStatus = "candidate" | "accepted" | "rejected";

type MaskBox = {
  id: string;
  pageNumber: number;
  x: number;
  y: number;
  width: number;
  height: number;
  source: MaskSource;
  status: MaskStatus;
  label?: string;
  confidence?: number;
  text?: string;
};
```

워크플로우 상태는 다음 개념을 포함한다.

```ts
type MaskingMode =
  | "idle"
  | "manual"
  | "autoProcessing"
  | "autoReview"
  | "autoEdit"
  | "manualFromOriginal";

type WorkingBase = "original" | "autoResult";

type MaskingWorkflowState = {
  mode: MaskingMode;
  workingBase: WorkingBase;
  masks: MaskBox[];
};
```

#### 구현 원칙

- 원본 PDF는 항상 보존한다.
- OCR 결과는 즉시 확정하지 않고 기본적으로 `candidate`로 둔다.
- 사용자가 승인한 OCR 결과만 최종 다운로드 대상에 포함할 수 있어야 한다.
- OCR 결과를 폐기하면 `source: "ocr"` 또는 `source: "regex"`인 박스를 제거하고 원본 기준 수동 마스킹으로 전환할 수 있어야 한다.
- 수동 박스와 자동 박스는 동일한 렌더링·삭제·다운로드 로직을 사용한다.

#### 완료 기준

- `MaskBox`가 수동/자동 탐지 결과를 모두 표현할 수 있다.
- 현재 작업 모드가 상태값으로 관리된다.
- 최종 다운로드 대상 `MaskBox[]`를 모드별로 산출할 수 있다.
- OCR 결과 폐기 후 원본 기준 수동 마스킹으로 전환할 수 있는 상태 구조가 준비된다.

---

### Step 5. 원본 기준 수동 마스킹 박스 생성 기능 구현

#### 작업 내용

사용자가 PDF 페이지 위에서 마우스로 드래그하여 수동 마스킹 박스를 생성할 수 있도록 한다.

처리 흐름은 다음과 같다.

```text
페이지 wrapper에서 mouseDown
→ 시작 좌표 저장
→ mouseMove 중 임시 박스 표시
→ mouseUp 시 최종 박스 생성
→ source: "manual", status: "accepted"인 MaskBox 생성
→ 해당 페이지 masks 배열 또는 공통 masks 배열에 저장
```

#### 구현 원칙

- 마스킹 박스는 검은색 또는 반투명 검은색으로 표시한다.
- 최소 크기 이하의 드래그는 무시한다.
- 박스 좌표는 해당 페이지의 실제 canvas 기준 좌표로 저장한다.
- 페이지 밖으로 벗어난 좌표는 보정한다.
- 처음에는 이동·리사이즈보다 생성·삭제를 우선한다.
- 수동 마스킹은 독립 기능이면서, 향후 OCR 결과 보완 기능으로도 재사용되어야 한다.

#### 완료 기준

- 각 페이지에서 드래그로 박스를 만들 수 있다.
- 생성된 박스가 화면에 표시된다.
- 페이지별로 박스가 독립 관리된다.
- 수동 생성 박스는 `source: "manual"`, `status: "accepted"`로 저장된다.
- 새 PDF를 업로드하면 기존 박스 상태가 초기화된다.

---

### Step 6. 마스킹 박스 삭제 및 상태 변경 기능 구현

#### 작업 내용

사용자가 잘못 생성한 마스킹 박스를 삭제하거나 자동 탐지 후보를 거절할 수 있도록 한다.

초기 구현 방식은 다음과 같다.

1. 박스 우측 상단에 X 버튼 표시
2. 수동 박스는 삭제 시 상태에서 제거
3. 자동 후보 박스는 삭제 또는 `status: "rejected"` 처리
4. 다운로드 대상 산정 시 `rejected` 박스 제외

#### 구현 원칙

- 수동 박스와 자동 박스 모두 동일 UI에서 삭제 가능해야 한다.
- 자동 박스는 삭제 대신 rejected 상태로 남길지, 실제 배열에서 제거할지 정책을 선택할 수 있어야 한다.
- 초기 MVP에서는 UI 단순성을 위해 화면에서는 제거된 것처럼 표시한다.
- 다운로드 대상은 `status: "accepted"`인 박스만 기본 포함한다.

#### 완료 기준

- 생성된 박스를 개별 삭제할 수 있다.
- 삭제 후 해당 페이지의 상태값에서 박스가 제거되거나 rejected 처리된다.
- 삭제된 박스는 PDF 생성 결과에 반영되지 않는다.
- 자동 탐지 후보와 수동 박스 모두 동일한 삭제 흐름을 사용할 수 있다.

---

### Step 7. 수동 마스킹 결과 다운로드 구현

#### 작업 내용

현재 선택된 수동 마스킹 박스를 기준으로 이미지 기반 PDF를 생성하고 다운로드한다.

처리 흐름은 다음과 같다.

```text
각 페이지 원본 렌더링 canvas 확보
→ 최종 적용 대상 masks 산정
→ 해당 페이지 masks 좌표에 검은 박스 그리기
→ canvas를 PNG 또는 JPEG로 변환
→ pdf-lib로 새 PDF 생성
→ 페이지별 이미지 삽입
→ PDF bytes 생성
→ Blob URL 생성
→ 다운로드
```

#### 구현 원칙

- 결과 PDF는 이미지 기반 PDF여야 한다.
- 원본 PDF의 텍스트 레이어는 유지하지 않는다.
- 마스킹 박스는 최종 이미지에 실제로 그려져야 한다.
- 미리보기용 canvas를 직접 훼손하지 않도록 복사 canvas를 사용한다.
- 페이지 크기는 렌더링된 canvas 비율을 유지한다.
- 이 다운로드 로직은 후속 자동 마스킹 결과에도 재사용 가능한 공통 함수로 만든다.

#### 완료 기준

- 다운로드된 PDF에서 수동 마스킹 영역이 실제로 검게 가려진다.
- PDF 텍스트 선택 또는 복사로 원본 텍스트가 노출되지 않는다.
- 모든 페이지가 누락 없이 포함된다.
- 마스킹 박스가 없는 페이지도 정상 포함된다.
- 후속 자동 마스킹 결과도 동일 함수에 `MaskBox[]`만 넘겨 다운로드할 수 있다.

---

### Step 8. OCR 서버/API 구조 설계

#### 작업 내용

스캔본 PDF 자동 마스킹을 위해 내부 NAS 서버 또는 컨테이너에서 OCR을 수행하는 API 구조를 설계한다.

처리 흐름은 다음과 같다.

```text
React 앱에서 자동 마스킹 선택
→ PDF 또는 페이지 이미지 전송
→ NAS OCR API 호출
→ 서버에서 PDF 페이지 이미지 변환
→ Tesseract OCR 실행
→ OCR 원시 결과 반환
→ 프론트엔드에서 MaskBox 후보로 표시
```

#### 구현 원칙

- 외부 OCR API는 사용하지 않는다.
- 우선 내부 서버 OCR Provider를 기본값으로 설계한다.
- 향후 외부 OCR API 또는 다른 OCR 엔진으로 교체할 수 있도록 Provider 인터페이스를 분리한다.
- OCR 처리 중 원본 PDF, 페이지 이미지, OCR 텍스트가 로그에 남지 않도록 한다.
- 서버 임시파일은 처리 종료 후 삭제하는 구조로 한다.

예상 Provider 구조는 다음과 같다.

```ts
type OcrProvider = "tesseract-local" | "external-api";

type OcrRequest = {
  fileId?: string;
  pages?: number[];
  provider: OcrProvider;
};

type OcrWord = {
  pageNumber: number;
  text: string;
  x: number;
  y: number;
  width: number;
  height: number;
  confidence: number;
};
```

#### 완료 기준

- OCR API 입력·출력 구조가 정의된다.
- OCR Provider 추상화 방향이 정리된다.
- 내부 Tesseract OCR을 기본 구현 대상으로 확정한다.
- 외부 API 사용은 별도 승인 대상으로 분리된다.

---

### Step 9. PDF 페이지 이미지 변환 및 Tesseract OCR 실행 구현

#### 작업 내용

NAS 서버 또는 컨테이너에서 PDF 페이지를 이미지로 변환하고, Tesseract OCR을 `kor+eng` 기준으로 실행한다.

처리 흐름은 다음과 같다.

```text
PDF 수신
→ 페이지별 이미지 변환
→ tesseract page.png stdout -l kor+eng tsv 실행
→ TSV 결과 수신
→ 단어별 텍스트·좌표·신뢰도 추출
```

#### 구현 원칙

- 좌표가 필요하므로 단순 텍스트 출력이 아니라 TSV 또는 hOCR 결과를 사용한다.
- 한글과 영문이 혼재될 수 있으므로 `kor+eng`을 기본 언어로 한다.
- OCR 결과의 좌표는 이미지 좌표 기준이므로, 프론트엔드 canvas 좌표와 변환 관계를 명확히 한다.
- 서버 임시파일은 처리 종료 후 삭제한다.
- OCR 처리 실패 시 사용자에게 실패 사유를 일반화하여 표시한다.
- OCR API 컨테이너에는 `tesseract-ocr`, `tesseract-ocr-kor`, `tesseract-ocr-eng`, `poppler-utils`를 설치한다.
- OCR 실행 전 `tesseract --version`, `tesseract --list-langs`, `pdftoppm -h`로 도구 설치 상태를 검증한다.

#### 완료 기준

- 스캔본 PDF 1페이지에 대해 OCR 텍스트를 추출할 수 있다.
- 단어별 좌표와 신뢰도를 확보할 수 있다.
- 다중 페이지 PDF에서 pageNumber가 유지된다.
- OCR 결과가 프론트엔드에 전달 가능한 JSON 구조로 변환된다.

---

### Step 10. 정규식 기반 개인정보 탐지 및 MaskBoxCandidate 생성

#### 작업 내용

OCR 원시 결과에서 정규식 기반으로 개인정보를 탐지하고, 탐지 결과와 실제 마스킹 후보를 분리해 생성한다.

탐지 규칙은 별도 파일로 관리한다. 사용자가 후속 단계에서 이 파일만 수정해 탐지 범위를 변경할 수 있도록 한다.

초기 탐지 대상은 다음 두 가지로 제한한다.

| 대상 | 예시 | 탐지 방식 |
|---|---|---|
| 주민등록번호 | 900101-1234567 | 정규식 |
| 주소 | 서울 서초구 XXXXXX | 정규식 |

#### 구현 원칙

- `Detection`은 무엇을 찾았는지 표현한다.
- `MaskBoxCandidate`는 실제로 어디를 가릴지 표현한다.
- OCR API 응답에서 `Detection[]`과 `MaskBoxCandidate[]`를 생성한다.
- Step 10에서는 UI 연결을 하지 않는다. 프론트엔드 `MaskBox` 변환과 사용자 승인·수정·삭제 흐름은 Step 11 이후에 연결한다.
- 모든 후보는 자동 확정하지 않고 `status: "review"`로 생성한다.
- 주민등록번호는 13자리 전체를 `Detection.rawText`로 보존하되, 기본 `MaskBoxCandidate`는 뒤 7자리 영역만 생성한다.
- OCR 단어 bbox가 주민등록번호 전체 하나로만 제공되는 경우 문자 인덱스 비율 기반으로 뒤 7자리 영역을 추정한다.
- 주소는 전체 주소를 `Detection.rawText`로 보존하되, `시`, `구`, `군` 이후 텍스트만 `MaskBoxCandidate`로 생성한다.
- 주소 OCR 결과가 여러 단어로 나뉘는 경우 같은 줄의 연속 단어를 합쳐 주소 후보를 만들고, 마스킹 구간에 해당하는 단어 bbox들을 합산해 후보 좌표를 만든다.
- 탐지된 텍스트의 bounding box는 관련 단어들의 좌표를 합산하여 산정한다.

#### 완료 기준

- 탐지 규칙 파일에서 주민등록번호와 주소 규칙을 분리 관리할 수 있다.
- OCR 결과에서 주민등록번호와 주소를 정규식으로 탐지할 수 있다.
- OCR API 응답에 `Detection[]`이 생성된다.
- OCR API 응답에 `MaskBoxCandidate[]`가 생성된다.
- 주민등록번호 후보는 뒤 7자리 영역만 대상으로 생성된다.
- 주소 후보는 `시`, `구`, `군` 이후 영역만 대상으로 생성된다.
- 모든 후보는 `review` 상태로 생성된다.

---

### Step 11. 자동 마스킹 결과 검토 UI 구현

#### 작업 내용

자동 OCR/정규식 탐지 결과를 사용자에게 검토 가능한 마스킹 후보로 표시한다.

사용자는 다음 작업을 할 수 있어야 한다.

1. OCR 결과 전체 확인
2. 자동 후보 박스 개별 삭제
3. 누락된 영역 수동 추가
4. 자동 후보 전체 승인
5. 자동 후보 전체 폐기
6. OCR 결과를 유지한 상태에서 수동 보완 진행

#### 구현 원칙

- 자동 후보는 수동 박스와 시각적으로 구분할 수 있어야 한다.
- 예: 자동 후보는 다른 테두리 스타일, 라벨, confidence 표시 등을 사용할 수 있다.
- 자동 후보가 많아도 화면이 과도하게 복잡해지지 않도록 해야 한다.
- 사용자가 검토 전 다운로드하려는 경우 경고 또는 확인 절차를 둔다.
- OCR 결과가 불완전할 수 있다는 점을 UI상 명확히 한다.

#### 완료 기준

- 자동 탐지 후보가 페이지 위에 표시된다.
- 사용자는 후보를 개별 삭제할 수 있다.
- 사용자는 후보를 그대로 다운로드 대상으로 사용할 수 있다.
- 사용자는 후보에 수동 마스킹을 추가할 수 있다.
- 사용자는 OCR 결과 전체를 폐기하고 원본 기준 수동 마스킹으로 전환할 수 있다.

---

### Step 12. 자동 결과 사용 방식 선택 기능 구현

#### 작업 내용

자동 마스킹 결과 확인 후 사용자가 다음 중 하나를 선택할 수 있도록 한다.

```text
1. OCR 결과 그대로 다운로드
2. OCR 결과에 수동 마스킹을 추가·삭제한 후 다운로드
3. OCR 결과를 폐기하고 원본 기준 수동 마스킹만 수행
```

#### 구현 원칙

- “OCR 결과 그대로 다운로드”는 자동 후보를 최종 적용 대상으로 본다.
- “OCR 결과 수정 후 다운로드”는 자동 후보와 수동 보완 박스를 함께 적용한다.
- “OCR 결과 폐기”는 자동 후보를 제거하고 원본 PDF 기준 수동 모드로 전환한다.
- 원본 PDF의 렌더링 상태는 유지되어야 한다.
- OCR 결과 폐기 후에도 사용자는 처음부터 다시 수동 마스킹을 진행할 수 있어야 한다.

#### 완료 기준

- 세 가지 자동 결과 처리 경로가 UI에서 선택 가능하다.
- 각 선택지에 따라 최종 적용 대상 `MaskBox[]`가 정확히 산정된다.
- OCR 결과 폐기 시 자동 후보가 다운로드 결과에 반영되지 않는다.
- OCR 결과 수정 시 자동 후보와 수동 보완 박스가 함께 반영된다.

---

### Step 13. 최종 다운로드 엔진 통합

#### 작업 내용

수동/자동/자동수정 여부와 관계없이 하나의 다운로드 엔진이 최종 선택된 `MaskBox[]`를 받아 이미지 기반 PDF를 생성하도록 통합한다.

공통 함수의 개념은 다음과 같다.

```ts
generateMaskedPdf({
  sourcePdf,
  pages,
  masks,
  fileName,
});
```

모드별 입력은 다음과 같다.

| 모드 | 다운로드 대상 masks |
|---|---|
| 수동 마스킹 | `source: "manual"` 및 `status: "accepted"` |
| 자동 결과 그대로 다운로드 | 자동 후보 중 다운로드 대상으로 확정된 박스 |
| 자동 결과 수정 후 다운로드 | 자동 박스 + 수동 보완 박스 |
| OCR 결과 폐기 후 수동 | 자동 박스 제외, 수동 박스만 적용 |

#### 구현 원칙

- 다운로드 로직은 중복 구현하지 않는다.
- 최종 적용 대상 박스 산정 함수와 PDF 생성 함수를 분리한다.
- 결과 PDF는 항상 이미지 기반 PDF로 생성한다.
- 원본 텍스트 레이어는 남기지 않는다.
- 다운로드 전 적용 대상 박스 수와 페이지 수를 검증한다.

#### 완료 기준

- 모든 모드에서 동일한 다운로드 엔진을 사용한다.
- 결과 PDF의 페이지 수가 원본과 일치한다.
- 최종 선택된 마스킹 박스만 결과 PDF에 반영된다.
- 삭제·거절된 박스는 결과 PDF에 반영되지 않는다.
- 결과 PDF에서 텍스트 선택·검색으로 원본 텍스트가 노출되지 않는다.

---

### Step 14. 다운로드 링크 제공 및 메모리·임시파일 정리

#### 작업 내용

생성된 PDF bytes를 Blob으로 변환하고, `URL.createObjectURL()`을 사용하여 다운로드 링크를 제공한다.

처리 흐름은 다음과 같다.

```text
pdfBytes 생성
→ Blob 생성
→ Object URL 생성
→ 다운로드 버튼 또는 링크 표시
→ 다운로드 완료 또는 새 작업 시작 시 Object URL 해제
→ OCR 서버 임시파일 삭제 확인
```

#### 구현 원칙

- 결과 PDF는 서버에 영구 저장하지 않는다.
- 다운로드 링크는 브라우저 세션성 링크로 제공한다.
- 새 파일 업로드 시 기존 다운로드 URL은 해제한다.
- 브라우저 창을 닫으면 결과물은 유지되지 않는다.
- OCR 서버를 사용하는 경우 임시파일 삭제 정책을 확인한다.

#### 완료 기준

- 사용자가 지정된 파일명으로 PDF를 다운로드할 수 있다.
- 새 작업 시작 시 기존 다운로드 링크가 정리된다.
- 브라우저 메모리 URL이 적절히 해제된다.
- OCR 임시파일이 서버에 남지 않는다.

---

### Step 15. 사용자 확인 및 안전장치 구현

#### 작업 내용

최종 다운로드 전 사용자가 마스킹 결과를 검토했음을 확인하는 절차와 안전장치를 둔다.

포함 기능은 다음과 같다.

1. 다운로드 전 검토 확인 체크박스
2. 자동 OCR 결과의 불완전 가능성 안내
3. OCR 후보가 남아 있을 때 다운로드 전 확인
4. 새로고침 또는 파일 교체 시 작업 손실 경고
5. 대용량 PDF 또는 OCR 처리 지연 경고

#### 구현 원칙

- 자동 탐지는 보조 기능이며 최종 검수 책임은 사용자에게 있음을 UI상 명확히 한다.
- 다운로드 전에는 적용 대상 마스킹 박스 수를 표시한다.
- OCR 결과가 없는 페이지도 있음을 표시할 수 있어야 한다.
- 사용자가 실수로 OCR 결과를 폐기하거나 파일을 교체하지 않도록 확인 절차를 둔다.

#### 완료 기준

- 다운로드 전 검토 확인 절차가 작동한다.
- 자동 탐지 결과의 한계가 사용자에게 표시된다.
- 새 파일 업로드 또는 새로고침 시 작업 손실 위험이 안내된다.
- 다운로드 직전 적용 대상 박스 수가 확인 가능하다.

---

## 5. 각 단계의 검증방법

### Step 1 검증방법: PDF 업로드 페이지 확인

| 검증 항목 | 검증 방법 | 통과 기준 |
|---|---|---|
| 파일 선택 | PDF 파일 선택 | 에러 없이 선택됨 |
| 파일명 표시 | 화면 표시 확인 | 선택 파일명 표시 |
| 파일 읽기 | ArrayBuffer 변환 | 콘솔 에러 없음 |
| 기존 UI 유지 | 화면 확인 | 기존 업로드 페이지 훼손 없음 |

현재 상태: 완료.

---

### Step 2 검증방법: PDF 페이지 렌더링

| 검증 항목 | 검증 방법 | 통과 기준 |
|---|---|---|
| 페이지 수 표시 | 1페이지, 다중 페이지 PDF 업로드 | 실제 페이지 수와 일치 |
| 페이지 순서 | 다중 페이지 PDF에서 좌우 버튼/숫자 입력으로 이동 | 원본 순서와 동일 |
| 렌더링 품질 | 확대 확인 | 주요 텍스트 판독 가능 |
| 에러 처리 | 손상 PDF 업로드 | 사용자에게 오류 표시 |
| 메모리 안정성 | 여러 PDF 반복 업로드 | 브라우저 멈춤 없음 |

현재 상태: 완료.

---

### Step 3 검증방법: 상태 구조

| 검증 항목 | 검증 방법 | 통과 기준 |
|---|---|---|
| 페이지별 상태 | 콘솔 또는 UI 확인 | 페이지별 상태 분리 |
| 박스 저장 구조 | `addMask`로 mock 박스 삽입 후 상태 확인 | 해당 페이지에만 저장 |
| 새 PDF 업로드 | 다른 PDF 업로드 | 기존 상태 초기화 |
| 좌표 일관성 | 좌표 변환 함수 확인 | 화면 좌표와 canvas 좌표 양방향 변환 가능 |

현재 상태: 완료.

---

### Step 4 검증방법: 공통 MaskBox 및 워크플로우 상태

| 검증 항목 | 검증 방법 | 통과 기준 |
|---|---|---|
| MaskBox 확장 | 타입 확인 | source/status/label/confidence/text 표현 가능 |
| 모드 관리 | 상태 전환 테스트 | manual/autoReview/autoEdit 등 전환 가능 |
| 원본 보존 | OCR 결과 폐기 시 확인 | 원본 기준 수동 모드 전환 가능 |
| 최종 masks 산정 | mock 데이터 테스트 | 모드별 다운로드 대상 정확 |

---

### Step 5 검증방법: 수동 마스킹 생성

| 검증 항목 | 검증 방법 | 통과 기준 |
|---|---|---|
| 드래그 생성 | 페이지 위 드래그 | 박스 표시 |
| 페이지별 생성 | 여러 페이지에서 생성 | 각 페이지에 독립 표시 |
| 최소 크기 처리 | 짧은 클릭 | 불필요 박스 생성 안 됨 |
| 경계 처리 | 페이지 밖으로 드래그 | 좌표가 페이지 내부로 보정 |
| source/status | 상태값 확인 | manual/accepted로 저장 |

---

### Step 6 검증방법: 박스 삭제 및 상태 변경

| 검증 항목 | 검증 방법 | 통과 기준 |
|---|---|---|
| 개별 삭제 | 특정 박스 삭제 | 선택 박스만 삭제 |
| 상태 반영 | 삭제 후 상태 확인 | masks 배열에서 제거 또는 rejected 처리 |
| 다운로드 반영 | 삭제 후 다운로드 | 삭제 박스는 결과 PDF에 없음 |
| 자동 후보 삭제 | mock OCR 박스 삭제 | 자동 후보도 동일하게 제외 |

---

### Step 7 검증방법: 수동 다운로드

| 검증 항목 | 검증 방법 | 통과 기준 |
|---|---|---|
| PDF 생성 | 다운로드 실행 | PDF 파일 생성 |
| 페이지 수 | 결과 PDF 열람 | 원본 페이지 수와 동일 |
| 마스킹 반영 | 마스킹 영역 확인 | 검은 박스로 표시 |
| 텍스트 레이어 제거 | 텍스트 선택 시도 | 원본 텍스트 선택 불가 |
| 원본 외형 유지 | 원본과 비교 | 레이아웃·비율 유지 |

---

### Step 8 검증방법: OCR API 구조

| 검증 항목 | 검증 방법 | 통과 기준 |
|---|---|---|
| API 입력 구조 | mock 요청 작성 | PDF/pages/provider 표현 가능 |
| API 출력 구조 | mock OCR 결과 작성 | pageNumber/text/좌표/confidence 표현 가능 |
| Provider 분리 | 코드 구조 확인 | tesseract-local 외 Provider 확장 가능 |
| 보안 구조 | 로그 확인 | PDF 텍스트·파일 내용 로그 없음 |

---

### Step 9 검증방법: Tesseract OCR 실행

| 검증 항목 | 검증 방법 | 통과 기준 |
|---|---|---|
| 이미지 변환 | PDF 1페이지 변환 | PNG/JPEG 생성 |
| OCR 실행 | kor+eng tsv 실행 | OCR 결과 반환 |
| 좌표 추출 | TSV 파싱 | 단어별 좌표 확보 |
| 다중 페이지 | 2페이지 이상 OCR | pageNumber 유지 |
| 임시파일 삭제 | 처리 후 서버 확인 | 임시파일 제거 |

---

### Step 10 검증방법: 정규식 탐지 및 MaskBox 변환

| 검증 항목 | 검증 방법 | 통과 기준 |
|---|---|---|
| 주민번호 탐지 | 샘플 OCR 텍스트 | 패턴 탐지 |
| 전화번호 탐지 | 샘플 OCR 텍스트 | 패턴 탐지 |
| 이메일 탐지 | 샘플 OCR 텍스트 | 패턴 탐지 |
| 좌표 결합 | 분리된 단어 테스트 | 하나의 MaskBox로 결합 |
| 후보 상태 | 결과 확인 | candidate로 생성 |

---

### Step 11 검증방법: 자동 결과 검토 UI

| 검증 항목 | 검증 방법 | 통과 기준 |
|---|---|---|
| 후보 표시 | mock OCR MaskBox 삽입 | 해당 페이지에 표시 |
| 후보 삭제 | 자동 박스 삭제 | 화면 및 상태에서 제외 |
| 수동 보완 | 후보 위/주변에 수동 박스 추가 | 함께 표시 |
| 전체 승인 | 버튼 실행 | 다운로드 대상 포함 |
| 전체 폐기 | 버튼 실행 | OCR 후보 제거 |

---

### Step 12 검증방법: 자동 결과 사용 방식 선택

| 검증 항목 | 검증 방법 | 통과 기준 |
|---|---|---|
| 그대로 다운로드 | 자동 후보만 적용 | 결과 PDF에 OCR 박스 반영 |
| 수정 후 다운로드 | 자동 + 수동 박스 적용 | 두 종류 모두 반영 |
| 폐기 후 수동 | OCR 후보 폐기 | 수동 박스만 반영 |
| 원본 보존 | 폐기 후 확인 | 원본 기준 수동 작업 가능 |

---

### Step 13 검증방법: 최종 다운로드 엔진 통합

| 검증 항목 | 검증 방법 | 통과 기준 |
|---|---|---|
| 공통 함수 사용 | 코드 확인 | 모드별 중복 다운로드 로직 없음 |
| 최종 masks 산정 | 모드별 mock 테스트 | 적용 대상 정확 |
| 페이지 수 | 결과 PDF 확인 | 원본과 동일 |
| 텍스트 레이어 제거 | 텍스트 선택 시도 | 원본 텍스트 선택 불가 |
| 제외 박스 반영 | rejected 박스 포함 테스트 | 결과 PDF에 미반영 |

---

### Step 14 검증방법: 다운로드 링크 및 정리

| 검증 항목 | 검증 방법 | 통과 기준 |
|---|---|---|
| 다운로드 링크 | 클릭 | PDF 다운로드 |
| 파일명 | 저장 파일 확인 | 지정 파일명 사용 |
| 새 작업 초기화 | 새 PDF 업로드 | 기존 링크·상태 정리 |
| Object URL 해제 | 개발자도구/코드 확인 | revokeObjectURL 호출 |
| OCR 임시파일 | 서버 확인 | 원본·이미지·OCR 임시파일 삭제 |

---

### Step 15 검증방법: 사용자 확인 및 안전장치

| 검증 항목 | 검증 방법 | 통과 기준 |
|---|---|---|
| 검토 확인 | 체크박스 미선택 후 다운로드 | 다운로드 제한 또는 경고 |
| OCR 한계 안내 | 자동 결과 화면 확인 | 안내 문구 표시 |
| 적용 박스 수 표시 | 다운로드 전 확인 | 최종 적용 박스 수 표시 |
| 작업 손실 경고 | 새 파일 업로드/새로고침 | 경고 표시 |
| 대용량 경고 | 큰 PDF 업로드 | 경고 또는 제한 표시 |

---

## 6. 위험구간

### 6.1 좌표계 불일치

#### 내용

화면에 표시되는 페이지 크기, PDF.js canvas 크기, OCR 이미지 크기, 최종 다운로드 canvas 크기가 다르면 마스킹 박스가 어긋날 수 있다.

#### 위험도

매우 높음

#### 대응방안

- 좌표는 기본적으로 실제 canvas 기준으로 저장한다.
- OCR 이미지 좌표와 프론트엔드 canvas 좌표의 변환 비율을 명시적으로 관리한다.
- 화면 표시 크기와 canvas 실제 크기의 scale ratio를 별도 함수로 계산한다.
- OCR 결과를 MaskBox로 변환할 때 좌표계 변환 테스트를 수행한다.
- 다운로드 전 샘플 PDF로 좌표 일치 여부를 검증한다.

---

### 6.2 OCR 좌표 품질 문제

#### 내용

Tesseract OCR 결과의 bounding box가 실제 민감정보 전체를 정확히 덮지 못할 수 있다.

#### 위험도

높음

#### 대응방안

- 탐지된 박스에 기본 padding을 부여한다.
- 인접 단어 결합 시 전체 bounding box를 넉넉하게 산정한다.
- 자동 결과는 확정값이 아니라 후보로 표시한다.
- 사용자가 자동 후보를 확인·수정할 수 있도록 한다.

---

### 6.3 OCR 인식률 한계

#### 내용

스캔 품질, 기울어진 문서, 낮은 해상도, 복잡한 표, 필기체 등으로 인해 OCR 인식률이 낮을 수 있다.

#### 위험도

높음

#### 대응방안

- OCR 결과를 반드시 사용자 검토 대상으로 둔다.
- 신뢰도 낮은 항목은 별도 표시한다.
- 전처리 기능은 후속 단계에서 검토한다.
- 자동 탐지 실패 시 원본 기준 수동 마스킹으로 전환할 수 있게 한다.

---

### 6.4 브라우저 및 서버 성능 문제

#### 내용

대용량 PDF, 고해상도 렌더링, OCR 처리로 인해 브라우저 또는 NAS 서버 성능 부담이 커질 수 있다.

#### 위험도

중간~높음

#### 대응방안

- 초기 scale은 2.0으로 제한한다.
- 대용량 PDF 경고 메시지를 둔다.
- OCR은 페이지별 순차 처리 또는 큐 방식으로 전환 가능하게 설계한다.
- 처리 가능한 최대 페이지 수와 파일 크기 제한을 둔다.
- Vite 빌드의 대형 chunk 경고는 현재 Step 8의 실패 사유가 아니며, 기능 및 타입 안정성을 우선한다.
- PDF.js lazy loading 또는 `manualChunks` 기반 번들 최적화는 별도 후속 성능 최적화 단계로 분리한다.

---

### 6.5 텍스트 레이어 잔존

#### 내용

결과 PDF가 이미지 기반이 아니라 원본 텍스트 레이어를 유지하면 마스킹 후에도 복사·검색으로 개인정보가 노출될 수 있다.

#### 위험도

매우 높음

#### 대응방안

- 결과 PDF는 새 PDF로 생성한다.
- 각 페이지는 이미지로만 삽입한다.
- 원본 PDF 페이지를 직접 수정하지 않는다.
- 결과 PDF에서 텍스트 선택이 불가능한지 검증한다.

---

### 6.6 서버 저장 또는 로그 노출

#### 내용

OCR 서버 도입 시 원본 PDF, 페이지 이미지, OCR 텍스트, 파일명, 탐지 결과가 서버 또는 로그에 남을 수 있다.

#### 위험도

매우 높음

#### 대응방안

- 서버 임시파일은 처리 종료 후 삭제한다.
- 로그에 파일명·텍스트·개인정보를 남기지 않는다.
- OCR 결과 저장 여부는 별도 승인 전까지 금지한다.
- 개발 로그에도 민감정보가 출력되지 않도록 한다.

---

### 6.7 외부 API 도입 시 개인정보 전송 이슈

#### 내용

향후 OCR API 또는 LLM API 사용 시 PDF 이미지 또는 텍스트가 외부 사업자에게 전송될 수 있다.

#### 위험도

높음

#### 대응방안

- 초기 자동 OCR은 내부 NAS 서버 OCR을 기본값으로 한다.
- 외부 API 사용은 별도 법무·보안·개인정보 검토 후 승인한다.
- 전송 데이터 최소화 원칙을 적용한다.
- API 제공자의 데이터 보관·학습 사용 정책을 확인한다.

---

### 6.8 자동 탐지 결과에 대한 과신

#### 내용

사용자가 OCR 자동 탐지 결과를 완전한 것으로 오인하고 검토 없이 다운로드할 수 있다.

#### 위험도

높음

#### 대응방안

- 자동 탐지는 후보임을 명확히 표시한다.
- 다운로드 전 검토 확인 절차를 둔다.
- 적용 대상 박스 수와 탐지 유형을 표시한다.
- OCR 결과가 없거나 신뢰도 낮은 페이지를 안내한다.

---

## 7. 사람의 승인이 필요한 지점

### Approval 1. ver.2.0 목표 범위 확정

#### 승인 필요 내용

최종 시스템을 다음 흐름으로 확정할지 승인해야 한다.

```text
수동 마스킹
자동 OCR 마스킹
자동 OCR 결과 그대로 다운로드
자동 OCR 결과 수정 후 다운로드
OCR 결과 폐기 후 원본 기준 수동 마스킹
```

#### 승인자 판단 필요 사항

- 수동/자동 선택 구조를 초기 설계에 포함할지 여부
- OCR 결과를 후보 상태로 두고 사용자 검토를 필수 흐름으로 둘지 여부

---

### Approval 2. 원본 PDF 보존 및 OCR 결과 폐기 가능 구조 확정

#### 승인 필요 내용

원본 PDF 상태를 항상 보존하고, OCR 결과가 마음에 들지 않을 경우 원본 기준 수동 마스킹으로 돌아갈 수 있도록 할지 승인해야 한다.

#### 승인자 판단 필요 사항

- OCR 결과 폐기 기능 필요 여부
- OCR 결과와 원본 기준 수동 작업을 상태상 분리할 필요성

---

### Approval 3. 내부 NAS OCR 서버 방식 확정

#### 승인 필요 내용

스캔본 PDF 자동 탐지를 위해 내부 NAS 서버 또는 컨테이너에서 Tesseract OCR을 실행하는 방식을 채택할지 승인해야 한다.

#### 승인자 판단 필요 사항

- 외부 OCR API가 아닌 내부 OCR 엔진을 우선 사용할지 여부
- NAS 서버 성능과 관리 부담을 수용할지 여부
- OCR 임시파일 삭제·로그 제한 정책 수용 여부

---

### Approval 4. 결과 PDF의 이미지 기반 출력 방식 확정

#### 승인 필요 내용

결과 PDF를 원본 텍스트 레이어 없는 이미지 기반 PDF로 생성하는 방식을 확정해야 한다.

#### 승인자 판단 필요 사항

- 결과 PDF에서 텍스트 선택·검색이 불가능해지는 것을 수용할지 여부
- 파일 크기 증가와 품질 저하 가능성을 수용할지 여부

---

### Approval 5. 마스킹 박스 UX 방식 확정

#### 승인 필요 내용

초기 마스킹 박스 조작 방식을 확정해야 한다.

선택지는 다음과 같다.

1. 드래그로 생성 + X 버튼으로 삭제
2. 드래그로 생성 + 선택 후 Delete 키 삭제
3. 드래그로 생성 + 이동·리사이즈까지 포함

#### 권장안

초기 구현은 다음 방식으로 한다.

```text
드래그로 생성
+ X 버튼으로 삭제
+ 이동·리사이즈는 후속 단계
```

---

### Approval 6. 자동 후보 처리 정책 확정

#### 승인 필요 내용

OCR 결과를 다음 중 어떤 방식으로 처리할지 확정해야 한다.

1. OCR 후보를 기본적으로 candidate로 표시
2. 사용자가 전체 승인 또는 개별 삭제 가능
3. 다운로드 시 candidate를 자동 포함할지, accepted만 포함할지 결정

#### 권장안

- OCR 결과는 기본적으로 `candidate`로 둔다.
- “OCR 결과 그대로 다운로드” 선택 시 candidate를 다운로드 대상으로 포함한다.
- 사용자가 삭제한 후보는 `rejected` 또는 제거 상태로 관리한다.

---

### Approval 7. 외부 API 사용 여부

#### 승인 필요 내용

향후 OCR API 또는 LLM API를 사용할 경우, 개인정보 외부 전송 가능성에 대한 별도 승인이 필요하다.

#### 승인자 판단 필요 사항

- 외부 API 사용 허용 여부
- 전송 가능 데이터 범위
- API 제공자의 데이터 보관·학습 사용 정책
- 사내 보안정책 및 개인정보 처리 기준 부합 여부

---

## 8. rollback 기준

### Rollback 1. PDF 렌더링 실패

#### 기준

다음 문제가 발생하면 PDF 렌더링 구현 직전 상태로 되돌린다.

- 기존 업로드 페이지가 정상 작동하지 않음
- PDF 업로드 후 화면이 멈춤
- 다중 페이지 PDF에서 렌더링 오류 발생
- 기존 UI 구조가 크게 훼손됨

#### 복구 기준

- 기존 PDF 업로드 페이지가 정상 표시되어야 한다.
- 파일 선택 기능이 정상 작동해야 한다.

---

### Rollback 2. 상태 구조 변경으로 기존 Step 1~3 기능 훼손

#### 기준

Step 4에서 MaskBox 및 Workflow 상태 구조를 확장한 후 다음 문제가 발생하면 Step 3 완료 상태로 되돌린다.

- PDF 업로드 후 페이지 상태가 생성되지 않음
- 페이지별 렌더링 결과가 표시되지 않음
- 기존 페이지별 상태 구조가 깨짐
- 새 PDF 업로드 시 상태 초기화가 되지 않음

#### 복구 기준

- Step 1~3의 업로드, 렌더링, 페이지별 상태 관리 기능이 정상이어야 한다.

---

### Rollback 3. 마스킹 박스 좌표 불일치

#### 기준

다음 문제가 발생하면 수동 마스킹 박스 기능 구현 직전 상태로 되돌린다.

- 화면상 박스 위치와 다운로드 PDF의 마스킹 위치가 다름
- 페이지 스크롤 또는 확대·축소 시 좌표가 어긋남
- 페이지별 박스가 다른 페이지에 적용됨
- OCR 좌표 변환 후 박스가 엉뚱한 위치에 표시됨

#### 복구 기준

- PDF 페이지 미리보기 기능은 정상 유지되어야 한다.
- 마스킹 기능은 비활성화하더라도 렌더링은 정상이어야 한다.

---

### Rollback 4. PDF 재생성 결과 불량

#### 기준

다음 문제가 발생하면 PDF 다운로드 기능 구현 직전 상태로 되돌린다.

- 생성된 PDF가 열리지 않음
- 페이지 일부가 누락됨
- 페이지 순서가 변경됨
- 마스킹 박스가 반영되지 않음
- 결과 PDF에 원본 텍스트 레이어가 남아 있음

#### 복구 기준

- 수동 마스킹 박스 UI는 정상 유지되어야 한다.
- PDF 생성 버튼은 임시 비활성화할 수 있다.

---

### Rollback 5. OCR 서버 처리 실패

#### 기준

다음 문제가 반복되면 자동 OCR 기능을 비활성화하고 수동 마스킹 기능만 유지한다.

- OCR 서버가 PDF를 처리하지 못함
- OCR 처리 중 서버 또는 컨테이너가 불안정해짐
- OCR 결과 좌표가 일관되게 부정확함
- OCR 처리 시간이 업무상 사용하기 어려운 수준임

#### 복구 기준

- 수동 마스킹 및 수동 다운로드 기능은 정상 유지되어야 한다.
- 자동 마스킹 버튼은 임시 비활성화할 수 있다.

---

### Rollback 6. 브라우저 또는 서버 성능 문제

#### 기준

다음 문제가 반복되면 고해상도 렌더링, 전체 페이지 동시 렌더링 또는 OCR 처리 방식을 되돌린다.

- 일반 PDF에서 브라우저가 멈춤
- 메모리 사용량이 과도함
- OCR 처리로 NAS 서버가 불안정해짐
- 페이지 렌더링 또는 OCR 시간이 업무상 사용하기 어려운 수준임

#### 복구 기준

- 렌더링 scale을 낮춘다.
- 페이지별 순차 렌더링 방식으로 전환한다.
- OCR 페이지 수 제한을 도입한다.
- 자동 OCR을 비활성화하고 수동 마스킹으로 우회한다.

---

### Rollback 7. 보안 정책 위반 가능성

#### 기준

다음 상황이 확인되면 즉시 해당 기능을 비활성화하거나 되돌린다.

- 원본 PDF가 서버에 영구 저장됨
- 결과 PDF가 서버에 영구 저장됨
- OCR 텍스트가 로그에 남음
- 개인정보가 포함된 페이지 이미지가 삭제되지 않음
- 사용자의 명시적 승인 없이 외부 API로 데이터가 전송됨

#### 복구 기준

- 브라우저 내 수동 처리만 남긴다.
- OCR 서버 기능을 비활성화한다.
- 외부 전송 코드를 제거한다.
- 로그 출력 코드를 제거한다.
- 임시파일 삭제 로직을 검증한다.

---

## 9. 결정 로그

### Decision 1. 최종 시스템은 수동/자동/자동결과수정 흐름을 모두 지원한다.

#### 결정

P2는 단순 수동 마스킹 도구가 아니라, 사용자가 수동 마스킹과 자동 OCR 마스킹을 선택하고, 자동 결과를 그대로 사용하거나 수동으로 보완하거나 폐기할 수 있는 구조로 설계한다.

#### 사유

- 실제 업무에서는 스캔본 PDF가 주 대상일 가능성이 높다.
- OCR 결과는 불완전할 수 있으므로 사용자 검수·수정 흐름이 필수다.
- 자동 결과가 만족스럽지 않은 경우 원본 기준 수동 작업으로 돌아갈 수 있어야 한다.

---

### Decision 2. 원본 PDF 상태는 항상 보존한다.

#### 결정

자동 OCR 결과와 관계없이 원본 PDF 상태를 유지하고, OCR 결과 폐기 후 원본 기준 수동 마스킹으로 전환할 수 있도록 한다.

#### 사유

- OCR 결과가 부정확한 경우 원본에서 다시 작업해야 한다.
- 자동 처리 결과를 원본처럼 취급하면 사용자 선택지가 제한된다.
- 결과물 검수 및 rollback에 유리하다.

---

### Decision 3. 수동 마스킹은 독립 기능이자 자동 OCR 결과 보정 기능으로 설계한다.

#### 결정

수동 마스킹 기능은 원본 기준 독립 기능으로 구현하되, 자동 OCR 결과에 누락된 영역을 추가하거나 잘못된 영역을 보완하는 공통 편집 기능으로 재사용한다.

#### 사유

- 수동 마스킹과 자동 결과 수정 UI를 별도로 만들면 중복이 커진다.
- OCR 결과 보완은 결국 MaskBox 추가·삭제와 동일한 문제다.
- 공통화하면 다운로드 로직도 단순해진다.

---

### Decision 4. OCR 결과는 즉시 확정하지 않고 후보 상태로 둔다.

#### 결정

OCR/정규식 탐지 결과는 기본적으로 `status: "candidate"`인 `MaskBox`로 저장한다.

#### 사유

- OCR은 인식 누락과 오탐 가능성이 있다.
- 사용자가 자동 결과를 확인·삭제·보완할 수 있어야 한다.
- 최종 다운로드 전에 사용자의 검수 흐름을 둘 수 있다.

---

### Decision 5. 결과 PDF는 이미지 기반 PDF로 생성한다.

#### 결정

원본 PDF를 직접 수정하지 않고, 각 페이지를 이미지로 렌더링한 후 마스킹 박스를 그려 새 PDF로 생성한다.

#### 사유

- 원본 텍스트 레이어 잔존에 따른 개인정보 노출 위험을 줄이기 위함이다.
- 사용자가 요구한 “이미지로 인쇄”와 유사한 결과를 얻기 위함이다.
- PDF 내부 구조 편집보다 구현 안정성이 높다.

---

### Decision 6. 자동 OCR은 내부 NAS 서버 기반 Tesseract 방식을 우선 검토한다.

#### 결정

스캔본 PDF 자동 탐지를 위해 외부 OCR API보다 내부 NAS 서버 또는 컨테이너에서 Tesseract OCR을 실행하는 구조를 우선 검토한다.

#### 사유

- 개인정보와 업무문서가 외부 서버로 전송되는 위험을 줄일 수 있다.
- 외부 API 사용은 별도 법무·보안·개인정보 검토가 필요하다.
- Tesseract TSV/hOCR 결과를 사용하면 텍스트뿐 아니라 좌표를 확보할 수 있다.

---

### Decision 7. OCR/정규식/수동 결과는 모두 동일한 MaskBox 구조를 사용한다.

#### 결정

수동 생성 박스, OCR 탐지 박스, 정규식 탐지 박스는 모두 `MaskBox` 구조로 변환하여 관리한다.

#### 사유

- 수동 박스와 자동 박스를 동일하게 렌더링·삭제·수정할 수 있다.
- 다운로드 PDF 생성 로직을 단일화할 수 있다.
- 기능 확장 시 구조 변경 부담을 줄일 수 있다.

---

### Decision 8. 다운로드 로직은 최종 선택된 MaskBox[]를 받는 공통 엔진으로 통합한다.

#### 결정

수동/자동/자동수정 여부에 따라 다운로드 함수를 나누지 않고, 최종 적용 대상 `MaskBox[]`를 산정한 뒤 하나의 PDF 생성 함수에 전달한다.

#### 사유

- 다운로드 로직 중복을 줄일 수 있다.
- 자동 결과 폐기, 수동 보완 등 분기 처리가 명확해진다.
- 테스트 범위를 줄이고 안정성을 높일 수 있다.

---

### Decision 9. 외부 OCR API와 LLM API는 별도 승인 전까지 제외한다.

#### 결정

외부 OCR API 및 외부 LLM API는 초기 구현 범위에서 제외하고, 내부 OCR 구조 안정화 후 별도 승인 절차를 거쳐 검토한다.

#### 사유

- 개인정보 외부 전송, 업무위탁, 국외이전, API 제공자의 보관·학습 정책 검토가 필요하다.
- API 키 관리 및 비용 문제가 있다.
- 현재 목표는 내부 처리 기반의 스캔본 PDF 자동 후보 생성이다.

---

## 질문 목록

다음 사항은 구현 전 또는 각 단계 착수 전 추가 확인이 필요하다.

### Q1. 자동 마스킹을 어느 시점부터 UI에 노출할 것인지

현재 권장안은 다음과 같다.

```text
1차: 수동 마스킹 + 수동 다운로드 완성
2차: 자동 마스킹 버튼 및 OCR 처리 연결
3차: 자동 결과 검토·수정·폐기 흐름 완성
```

확인 필요 사항:

- 수동 다운로드 완성 전에도 자동 마스킹 UI의 자리만 미리 보여줄 것인가?
- 자동 마스킹 버튼은 OCR 서버 준비 전까지 비활성화할 것인가?

---

### Q2. OCR 결과의 기본 상태

확인 필요 사항:

- OCR 결과를 기본적으로 `candidate`로 둘 것인가?
- “자동 결과 그대로 다운로드” 선택 시 candidate를 자동 포함할 것인가?
- 아니면 사용자가 “전체 승인”을 눌러야 accepted로 전환되게 할 것인가?

권장안:

```text
OCR 결과는 candidate로 표시
사용자가 그대로 다운로드를 선택하면 candidate를 다운로드 대상으로 포함
개별 삭제한 후보는 제외
```

---

### Q3. OCR 결과 폐기 방식

확인 필요 사항:

- OCR 결과 폐기 시 자동 후보를 배열에서 완전히 제거할 것인가?
- 아니면 `status: "rejected"`로 보관하되 화면과 다운로드에서 제외할 것인가?
- 폐기 후 다시 자동 OCR을 실행할 수 있게 할 것인가?

---

### Q4. 마스킹 박스 이동·크기 조절 포함 여부

현재 권장안은 다음과 같다.

```text
1차: 드래그 생성 + 삭제
후속 단계: 이동 + 크기 조절
```

확인 필요 사항:

- 초기부터 박스 이동 기능이 필요한가?
- 초기부터 박스 크기 조절 기능이 필요한가?
- OCR 결과 보정에는 이동·리사이즈가 꼭 필요한가?

---

### Q5. PDF 렌더링 품질 기준

확인 필요 사항:

- 기본 렌더링 scale을 2.0으로 시작해도 되는가?
- 결과 PDF의 파일 크기가 증가해도 품질을 우선할 것인가?
- OCR용 이미지와 화면 미리보기용 이미지를 같은 scale로 둘 것인가?

---

### Q6. 처리 가능한 PDF 용량 또는 페이지 수 제한

확인 필요 사항:

- 1차 MVP에서 PDF 최대 용량 제한을 둘 것인가?
- 최대 페이지 수 제한을 둘 것인가?
- OCR 처리 가능 페이지 수를 별도로 제한할 것인가?
- 대용량 PDF 업로드 시 경고만 표시할 것인가, 업로드 자체를 막을 것인가?

---

### Q7. 다운로드 파일명 규칙

확인 필요 사항:

- 기본 파일명을 `masked.pdf`로 할 것인가?
- 원본 파일명을 활용하여 `원본파일명_masked.pdf`로 할 것인가?
- 자동 마스킹 결과와 수동 마스킹 결과 파일명을 구분할 것인가?
- 한글 파일명 처리를 허용할 것인가?

---

### Q8. 마스킹 색상 및 방식

확인 필요 사항:

- 기본 마스킹 색상을 검은색으로 할 것인가?
- 흰색, 회색, 블러 처리 옵션도 필요한가?
- 출력물 기준으로 완전 비가역적인 검은 박스 방식을 기본으로 할 것인가?

권장안:

```text
초기 구현은 검은색 박스만 사용
블러·반투명·흰색 옵션은 후속 검토
```

---

### Q9. 결과 PDF의 텍스트 선택 불가 요건

확인 필요 사항:

- 결과 PDF에서 텍스트 검색·선택이 불가능해지는 것을 확정 요건으로 둘 것인가?
- 원본 PDF의 접근성 또는 검색 가능성은 포기해도 되는가?

권장안:

```text
개인정보 마스킹 목적상 결과 PDF는 이미지 기반으로 생성
텍스트 선택·검색 불가를 보안 요건으로 둠
```

---

### Q10. 내부 OCR 서버 구성 방식

확인 필요 사항:

- 기존 devbox 컨테이너에 OCR 패키지를 설치할 것인가?
- 별도 OCR API 컨테이너를 만들 것인가?
- OCR 서버와 React/Vite 앱의 포트 및 네트워크 구성을 어떻게 할 것인가?
- OCR 임시파일 디렉터리와 삭제 정책은 어떻게 둘 것인가?

권장안:

```text
개발 초기에는 별도 OCR API 컨테이너 또는 서버 모듈로 분리
Tesseract, kor/eng 언어 데이터, poppler-utils 설치
임시파일은 작업별 폴더 생성 후 처리 종료 시 삭제
```

---

### Q11. OCR 언어 및 전처리

확인 필요 사항:

- 기본 OCR 언어를 `kor+eng`으로 둘 것인가?
- 숫자 중심 문서에서 별도 OCR 설정을 둘 것인가?
- 기울어진 스캔본, 저해상도 스캔본에 대한 전처리를 도입할 것인가?

---

### Q12. LLM/API 사용 정책

후속 단계에서 확인할 사항이다.

확인 필요 사항:

- 개인정보가 포함된 텍스트를 외부 LLM API에 전송할 수 있는가?
- 후보 텍스트와 일부 문맥만 전송하는 방식은 허용 가능한가?
- 사내망 또는 로컬 LLM 사용 가능성을 검토할 것인가?
- LLM은 좌표 산정이 아니라 판단 보조로만 제한할 것인가?

---

### Q13. 최종 다운로드 전 사용자 확인 절차

확인 필요 사항:

- 다운로드 전 “마스킹 결과를 확인했습니다” 체크박스를 둘 것인가?
- 자동 탐지 기능 도입 후에는 사용자의 최종 검수 확인을 필수화할 것인가?
- OCR 결과가 없는 페이지에 대한 별도 경고가 필요한가?

---

### Q14. 브라우저 창 종료 시 데이터 정리 방식

확인 필요 사항:

- 단순히 브라우저 메모리 상태로만 관리하면 되는가?
- 새로고침 또는 창 닫기 전 경고 메시지를 표시할 것인가?
- 작업 중 실수로 새로고침하는 경우 복구 불가 정책을 명확히 표시할 것인가?

---

### Q15. Step 4 착수 전 Codex 지시 범위

확인 필요 사항:

- Step 4에서는 실제 UI 변경을 최소화하고 상태 구조만 보완할 것인가?
- 아니면 수동/자동 선택 버튼의 UI 골격까지 추가할 것인가?

권장안:

```text
Step 4는 상태 구조와 workflow mode 설계 보완 중심
UI는 최소한의 debug 표시 또는 비활성 버튼 수준으로 제한
실제 마스킹 박스 생성 UI는 Step 5에서 구현
```
